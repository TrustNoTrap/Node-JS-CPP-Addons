{
  "scripts": {
    "configure": "npx node-gyp configure",
    "build": "npx node-gyp build",
    "test:build-js": "npm run build && node index.js",
    "test:js": "node index.js"
  },
  "dependencies": {
    "node-gyp": "^11.2.0"
  }
}
